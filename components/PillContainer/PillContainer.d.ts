import { default as React } from '../../../node_modules/react';

interface PillContainerProps {
    singleLine?: boolean;
    maxWidth: number;
}
export declare const PillContainer: React.MemoExoticComponent<({ singleLine, maxWidth }: PillContainerProps) => import("react/jsx-runtime").JSX.Element>;
export {};
