import { default as React } from '../../../../node_modules/react';
import { Sort } from '../../../../../../../../src/types';

interface SortItemProps {
    sort: Sort;
    index: number;
}
export declare const SortItem: React.MemoExoticComponent<({ sort, index }: SortItemProps) => import("react/jsx-runtime").JSX.Element>;
export {};
